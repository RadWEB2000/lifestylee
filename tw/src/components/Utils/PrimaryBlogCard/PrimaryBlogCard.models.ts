export type tPrimaryBlogCard = {
  category: string;
  release: string;
  excerpt: string;
  image: {
    altText: string;
    sourceUrl: string;
    srcSet: string;
    title: string;
  };
  slug: string;
  subdomain: string;
  title: string;
  uri: string;
};
